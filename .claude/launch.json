{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "ARC Pi Assistant (via SSH)",
      "runtimeExecutable": "ssh",
      "runtimeArgs": ["-p", "2222", "simon@192.168.68.159", "sudo systemctl restart arc && journalctl -u arc -f"],
      "port": 8765
    },
    {
      "name": "ARC Local Dev (uvicorn)",
      "runtimeExecutable": "python3",
      "runtimeArgs": ["-m", "uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8765", "--reload"],
      "port": 8765
    }
  ]
}
